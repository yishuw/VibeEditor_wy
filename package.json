{
  "name": "vibeeditor",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "VibeEditor - An AI-powered code editor with Monaco Editor, supporting both server and Electron deployment",
  "workspaces": [
    "packages/agent",
    "packages/core",
    "packages/server",
    "packages/electron",
    "packages/web"
  ],
  "scripts": {
    "dev:server": "npm run build:agent && npm run build:core && npm run dev -w packages/server",
    "dev:web": "npm run build:agent && npm run build:core && npm run dev -w packages/web",
    "dev:electron": "npm run build:agent && npm run build:core && npm run build:electron && concurrently \"npm run dev -w packages/server\" \"npm run dev -w packages/web\" \"npm run dev -w packages/electron\"",
    "dev:all": "npm run build:agent && npm run build:core && concurrently \"npm run dev -w packages/server\" \"npm run dev -w packages/web\"",
    "build:agent": "npm run build -w packages/agent",
    "build:core": "npm run build -w packages/core",
    "build:web": "npm run build -w packages/web",
    "build:server": "npm run build -w packages/server",
    "build:electron": "npm run build -w packages/electron",
    "build:all": "npm run build:agent && npm run build:core && npm run build:web && npm run build:server && npm run build:electron",
    "lint": "eslint packages/*/src --ext .ts,.tsx,.vue",
    "typecheck": "tsc -b",
    "cli": "npx tsx packages/agent/src/cli.ts",
    "mcp:cli": "npx tsx packages/agent/src/cli.ts",
    "mcp:test": "node --import tsx packages/agent/src/mcp/__test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "@typescript-eslint/eslint-plugin": "^6.19.0",
    "@typescript-eslint/parser": "^6.19.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "chart.js": "^4.5.1"
  }
}
